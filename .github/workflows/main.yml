name: Assignment Action

on:
  workflow_dispatch: # This allows you to run it manually

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24f3004422@ds.study.iitm.ac.in
        run: echo "Hello, world!"
